# AGENTS.md

任何时候使用中文，除非用户明确要求英文输出。

## Mid-Project Adoption Constitution

- 架构优先、设计优先、真源优先、严格验收。
- 本仓库是半路接管项目：已有代码、文档、脚本、配置、部署习惯或未提交变更都可能是真实边界；禁止把它当空项目重写。
- 编码前必须先做只读接管审计：读当前代码、入口、调用链、接口契约、生成规则、测试脚本、git 状态、已有 `docs/`/README/issue/review 线索。
- 当前项目使用 `docs/` 作为过渡真源目录；不得因为缺少 `dev-docs/` 就拒绝工作，也不得擅自宣布 `dev-docs/` 为唯一真源。
- 用户问边界时，先回答属于哪一层、哪一个 plane、哪个合同或哪个产品边界，再决定是否需要改代码。
- 用户说 `先看`、`不动代码`、`先给判断` 时，必须保持只读审查，直到用户明确要求执行。
- 禁止补丁式开发：不得用散落 if、临时兼容分支、硬编码、sleep、全局 flag、prompt 文案或 mock 绕过架构问题。
- 禁止无意义向后兼容：旧字段、旧接口、旧产品残影若污染当前合同，应先提产品取舍，不擅自保留双路径。
- 危险接管动作必须先让用户确认：创建或改写本文件、宣布 `dev-docs` 为唯一真源、迁移/归档/删除旧文档、重定产品语义、删除旧 API/字段/路由、创建嵌套 `dev-docs` git 仓库、批量重写公开 README/docs/UI copy。
- 用户明确否定的概念必须从代码、文档、计划和命名中删除，禁止换个名字继续保留。
- 若发现本文件或旧规则技术上错误，必须说明当前证据、推荐修正和影响范围，先让用户确认后再改。
- 共享语义必须单一真源：跨 UI/API/SDK/运行时复用的概念必须进入共享合同或核心层。
- adapter/controller/UI 只做映射和展示，不拥有核心业务语义。
- 每阶段必须有目的、收益、边界、验收命令和停止条件。
- 代码、接口、配置、架构边界或用户行为变化后必须同步文档。
- 验收结论必须基于当前命令、测试、日志、设备或用户侧证据；不得用旧记忆或旧输出代替。
- 当上下文过大或需要换窗口继续时，必须产出可直接复制的新窗口交接文本，包含当前 git 状态、已改内容、验证结果、未闭合风险、漂移警告和下一步命令。

## Current Truth

优先级：

1. 当前源码、测试、脚本、生成合同、运行日志、实机/用户侧证据、当前 git 状态。
2. 本文件。
3. `docs/README.md` 和当前项目确认的真源索引。
4. 当前 active architecture / acceptance / current-state audit 文档。
5. `docs/`、README、issue、review comments、commit history 中仍被当前代码或用户确认引用的事实。
6. archive 历史文档和旧会话，只能作为模式证据，不能覆盖当前仓库事实。

## Required Adoption Workflow

```text
只读接管审计
  -> 项目阶段分类
  -> agent 宪法确认
  -> 内部真源索引
  -> 当前状态审计
  -> owner map / 架构边界
  -> 验收与停止条件
  -> 第一个安全任务
```

## Git

- 提交前确认 git root、`git status --short`、ignored 文件、staged file list。
- 禁止 `git add .`。
- 禁止 force-add ignored 文件，除非用户明确点名路径并要求。
- 若存在 `dev-docs/.git` 或其他嵌套仓库，父仓和子仓分别验收、分别 staged、分别提交。
